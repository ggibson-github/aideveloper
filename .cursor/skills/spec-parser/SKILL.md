---
name: spec-parser
description: >-
  Parses the specification, extracts requirements, validates, lists assumptions
  and open questions. Use at project start or when spec changes.
---

# Spec Parser

## When to use

- `next_action` is `run spec-parser`
- User points to spec or spec changed

## Instructions

See [references/workflow.md](references/workflow.md).

**Read first:** `journal/state.json`, `spec.md`, `docs/facts/INDEX.md`.

Dual-write journal + state after completion. Set `allowed_reads` for hld-writer phase.
