---
description: Operator status — no design doc reads
---

Read `journal/state.json` only. Summarize: `next_action`, `phase`, `gates_pending`, `blockers`, `blocking_questions`, `last_verify`, stale artifacts from `docs/manifest/staleness.json` if present. Optionally read `docs/operator/dashboard.md`. Do not start implementation.
