# Program status (/program)

Show multi-workstream program state without reading full design docs.

Read `journal/state.json` `program` block and each `program/workstreams/<id>/lane.json`.

Run:

```bash
python scripts/generate-dashboard.py
```

See `docs/operator/dashboard.md` Program section.
