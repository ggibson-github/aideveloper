---
description: Resume pipeline — one step from journal/state.json
---

Read `journal/state.json` and `journal/progress.md`. Run the **continue** skill. Perform exactly **one** pipeline step from `next_action`. Dual-write journal and state after. Do not read files outside `allowed_reads`.
