# Blender export playbook (stub)

1. Open character mesh; verify scale per manifest.
2. Export FBX with manifest export settings.
3. Run evidence: `python scripts/validate-workflow.py` as smoke or custom export script.
