# Specification

Put your project requirements or specification here.

You can also tell the agent which file contains the spec (e.g. a path to another markdown file). The agent will parse this document and drive the pipeline: requirements summary → high-level design → detailed design → diagrams → task list → scaffold → implementation.
