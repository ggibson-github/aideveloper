<!-- Complete pass 3 2026-06-28 F3.4 -->

# F3.4: game studio goal_verify asset engine tests perf

**Parent:** [F3-index](F3-index.md) · **Branch F** · **Vision §8** · **Release:** v2.20

## Reader narrative
<!-- prose-source: agent plane-f 2026-06-28 -->

Game studio goal_verify proves assets load in engine, automated tests pass, and performance budgets hold—beyond per-task pytest evidence. The suite aggregates tool-operator logs, engine test output, and perf counters into one `goal.verify_command` resolved at scope complete ([A2.4](A2.4-goal-scope-complete-run-goal-verify.md)).

Weak goal_verify (smoke import only) is an accepted risk documented in adversarial review—pack authors tighten suites over releases. Failure blocks H3 and packages H2 with evidence paths attached. Extends generic pack verify ([F1.8](F1.8-pack-verify-goal-verify-suites.md)) without replacing task-level verify-router discipline.

## Purpose

F3.4 defines game studio goal verify asset engine tests perf for the agent-driven expert system. Organization — template-packs as whole-company ceiling.
## Scope

- Owns `F3.4` only; siblings under `F3` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §8 — Branch F — Organization plane (template-packs = ceiling)](../../full-automation-vision-and-hierarchy.md#8-branch-f-organization-plane-template-packs-ceiling).

```
│   └── F3.4 goal_verify: asset in engine + tests + performance budget
```
## Behavior / step logic
<!-- timeline-source: agent cli-composer-2.5 2026-06-28 -->

1. When platform promotion lands playbooks, scripts, or pack fragments, reconcile-stale registers each reusable path in docs/manifest/staleness.json so downstream task cards and compose hits can mark dependencies stale.
2. On script or playbook change during pursuit, the staleness graph propagates to dependent task cards and [E2.2](E2.2-compose-query-catalog-list-components.md) compose results—workers reconcile before advancing implement steps.
3. Regeneration of [E1.1](E1.1-catalog-scripts-manifest-generated.md) and [E1.7](E1.7-catalog-platform-catalog-md-umbrella.md) reconciles graph entries with live catalog INDEX rows after platform drain completes.
4. Pack verify suites from [F1.8](F1.8-pack-verify-goal-verify-suites.md) may assert graph completeness at goal_verify—missing nodes fail closed rather than allowing pursuit on orphan artifact paths.
5. If a promoted artifact lacks a staleness node or graph drift leaves dependents unmarked, pursuit stops at H2 until reconcile-stale or [D4.5](D4.5-platform-work-catalog-regeneration.md) restores traceability.

## JSON example

```json
{
  "node": "F3.4",
  "description": "game studio goal verify asset engine tests perf",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `template-packs/`
- `program/integration/manifest.md`
- `.cursor/skills/program-scoper/`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Pack role handoff while lane lease held â€” complete-work-order releases lease before role switch.
- Edge case `F3.4` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `F3.4`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Add `company.yaml` + `roles/*.yaml` to template-packs schema.
2. program-scoper selects pack; sets state.company.active_role.
3. Per-role allowed_reads in lane.json work orders.
4. Validate `F3.4` against SEC-15 release checklist and parent index links.
5. Document `F3.4` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `F3.4`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids F3.4` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §8 | Master hierarchy |
| [F3-index](F3-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids F3.4` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [F3-index](F3-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
