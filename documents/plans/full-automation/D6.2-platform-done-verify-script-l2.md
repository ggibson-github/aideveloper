<!-- Complete pass 3 2026-06-28 D6.2 -->

# D6.2: platform done verify script L2+

**Parent:** [D6-index](D6-index.md) · **Branch D** · **Vision §6** · **Release:** v2.16

## Reader narrative
<!-- prose-source: agent plane-d 2026-06-28 -->

For L2 and above promotions, definition of done requires a verify path—unit test, verify-router command, or validate-workflow hook—that proves the script or tool behaves as specified. L1 playbooks may cite manual verify until scripted; L2+ without automated verify fails platform done.

Evidence discipline mirrors product tasks but writes platform-appropriate logs under `evidence/` or unit test output. Script promotion pairs extraction ([D4.2](D4.2-platform-work-script-extraction.md)) with test file in `tests/unit/`. False done without verify triggers audit re-enqueue and blocks [D2.3](D2.3-dequeue-platform-turn-not-product.md) head advance.

## Purpose

D6.2 defines platform done verify script l2 for the agent-driven expert system. Platform evolution — promotion ladder, parallel queue, reuse.
## Scope

- Owns `D6.2` only; siblings under `D6` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §6 — Branch D — Platform evolution plane (parallel queue)](../../full-automation-vision-and-hierarchy.md#6-branch-d-platform-evolution-plane-parallel-queue).

```
├── D6.2 verify script for tool (if L2+)
```
## Behavior / step logic
<!-- timeline-source: agent cursor-agent 2026-06-28 -->

1. For L2 and above promotions, definition of done requires a verify path—unit test, verify-router command, or validate-workflow hook—that proves the script or tool behaves as specified.
2. Script promotion pairs [D4.2](D4.2-platform-work-script-extraction.md) extraction with a test file in `tests/unit/` before the conductor dequeues the platform item.
3. L1 playbooks may cite manual verify until scripted; L2+ without automated verify fails platform done and blocks [D2.3](D2.3-dequeue-platform-turn-not-product.md) head advance.
4. Verifier writes platform-appropriate logs under `evidence/` or captures unit test output; conductor dual-writes `last_verify` before marking promotion done.
5. False done without verify triggers audit re-enqueue and pursuit stops at H2 until automated proof exists.

## JSON example

```json
{
  "node": "D6.2",
  "description": "platform done verify script l2",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `docs/playbooks/`
- `scripts/`
- `.cursor/skills/playbook-keeper/`
- `state.platform.promotion_queue`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Platform queue depth 0 but product blocked on missing playbook â€” D3.3 priority cut skips platform drain.
- Edge case `D6.2` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `D6.2`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Add `platform.promotion_queue[]` to state.json schema.
2. Scheduler in autopilot workflow: `(steps_total % K) == 0` â†’ platform turn.
3. playbook-keeper + script extraction skills dequeue promotion items.
4. Validate `D6.2` against SEC-15 release checklist and parent index links.
5. Document `D6.2` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `D6.2`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids D6.2` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §6 | Master hierarchy |
| [D6-index](D6-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids D6.2` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [D6-index](D6-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
