<!-- Complete pass 3 2026-06-28 F2.2 -->

# F2.2: company spawn workstream department role lane

**Parent:** [F2-index](F2-index.md) · **Branch F** · **Vision §8** · **Release:** v2.19

## Reader narrative
<!-- prose-source: agent plane-f 2026-06-28 -->

After pack bind, program-scoper spawns program workstreams mapped to departments or role lanes—each lane gets lane.json leases, work orders, and spawn_policy compatible with integration manifest. Department boundaries align with pack roles ([F1.2](F1.2-pack-roles---yaml.md)); parallel lanes require manifest gate clearance ([F1.4](F1.4-pack-manifest-md-integration-contracts.md)).

orchestrate-program spawns workers only for ready lanes with valid leases ([C4.1](C4.1-workstreams-lane-json-leases.md)). Spawn without manifest approval violates program pipeline order. Lane spawn is S0 where lane scripts exist; otherwise conductor invokes program-scoper skill with pack context.

## Purpose

F2.2 defines company spawn workstream department role lane for the agent-driven expert system. Organization — template-packs as whole-company ceiling.
## Scope

- Owns `F2.2` only; siblings under `F2` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §8 — Branch F — Organization plane (template-packs = ceiling)](../../full-automation-vision-and-hierarchy.md#8-branch-f-organization-plane-template-packs-ceiling).

```
│   ├── F2.2 spawn program with workstream = department or role lane
```
## Behavior / step logic
<!-- timeline-source: agent cli-composer-2.5 2026-06-28 -->

1. Each promotion item in `state.platform.promotion_queue` carries age; after S0 preflight the conductor compares oldest backlog age to `drain_policy.max_platform_backlog_age` from the active pack ([D5.1](D5.1-configure-task-level-params-only.md)).
2. When oldest backlog exceeds the cap, force-drain policy schedules platform turns until age falls below the threshold or the queue empties—even if [D3.1](D3.1-1-platform-turn-per-k-product-turns.md) K-step cadence would defer the next platform drain.
3. Force drain is the adversarial-review backstop across D-branch scheduling: it prevents promotion debt from rotting during long product-only sprints without overriding active product evidence gates or clearing H2 blockers.
4. Partial promotion with re-enqueue resets item age; marking promotion done without passing [D6](D6-index.md) checklist fails closed and reopens the queue item at H2.
5. If force drain would starve evidence-blocked product work, the operator tunes age and force intensity per pack, or [D3.3](D3.3-priority-cut-product-blocked-missing-tool.md) suspends drain until the missing artifact class is enqueued.

## JSON example

```json
{
  "node": "F2.2",
  "description": "company spawn workstream department role lane",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `template-packs/`
- `program/integration/manifest.md`
- `.cursor/skills/program-scoper/`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Pack role handoff while lane lease held â€” complete-work-order releases lease before role switch.
- Edge case `F2.2` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `F2.2`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Add `company.yaml` + `roles/*.yaml` to template-packs schema.
2. program-scoper selects pack; sets state.company.active_role.
3. Per-role allowed_reads in lane.json work orders.
4. Validate `F2.2` against SEC-15 release checklist and parent index links.
5. Document `F2.2` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `F2.2`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids F2.2` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §8 | Master hierarchy |
| [F2-index](F2-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids F2.2` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [F2-index](F2-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
