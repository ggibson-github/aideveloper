<!-- Complete pass 3 2026-06-28 F6.2 -->

# F6.2: role allowed_reads scoped playbooks lane tasks

**Parent:** [F6-index](F6-index.md) · **Branch F** · **Vision §8** · **Release:** v2.19

## Reader narrative
<!-- prose-source: agent plane-f 2026-06-28 -->

Per-role `allowed_reads` scopes workers to role playbooks, lane task cards, facts entries, and design slices declared in pack YAML—not the full vision monolith or unrelated department lanes. Librarian returns paths within this cap (max five) before implement spawn ([B2.2](B2.2-librarian-allowed-reads-catalog-composition.md)).

Scope bleed (QA worker reading finance playbooks) violates pack intent and risks cross-department edits. allowed_reads updates on active_role rotation ([F6.1](F6.1-role-mapping-conductor-context-switch-active-role.md)). Lane.json work orders may further narrow reads for parallel slots. Forbidden paths are explicit deny list alongside allowed_reads.

## Purpose

F6.2 defines role allowed reads scoped playbooks lane tasks for the agent-driven expert system. Organization — template-packs as whole-company ceiling.
## Scope

- Owns `F6.2` only; siblings under `F6` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §8 — Branch F — Organization plane (template-packs = ceiling)](../../full-automation-vision-and-hierarchy.md#8-branch-f-organization-plane-template-packs-ceiling).

```
├── F6.2 allowed_reads scoped to role playbooks + lane tasks
```
## Behavior / step logic
<!-- timeline-source: agent cli-composer-2.5 2026-06-28 -->

1. When staleness.json or a program artifact graph flags stale nodes on paths in the next implement scope, the conductor invokes reconcile-stale or reconcile-artifact-graph before spawning workers—not after partial edits land.
2. S0 scripts maintain stale flags file-derived; the reconcile skills decide whether to rerun the affected design phase, waive with H2 operator approval, or enqueue hierarchy deepen items for impacted ids.
3. Workers on affected paths pause while reconciliation runs; pursuit resumes only after flags clear or an explicit H2 waiver is dual-written to journal and state.json.
4. In program mode, reconcile-artifact-graph consults [C4.4](C4.4-artifact-graph-per-program-and-pack.md) cross-stream contracts alongside [E5.1](E5.1-staleness-design-graph-staleness-json.md) as the design-graph source of truth.
5. If implement spawn would proceed against stale design without reconciliation or waiver, pursuit halts at H2—silent implement violates Plane E contract and invalidates goal_verify evidence.

## JSON example

```json
{
  "node": "F6.2",
  "description": "role allowed reads scoped playbooks lane tasks",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `template-packs/`
- `program/integration/manifest.md`
- `.cursor/skills/program-scoper/`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Pack role handoff while lane lease held â€” complete-work-order releases lease before role switch.
- Edge case `F6.2` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `F6.2`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Add `company.yaml` + `roles/*.yaml` to template-packs schema.
2. program-scoper selects pack; sets state.company.active_role.
3. Per-role allowed_reads in lane.json work orders.
4. Validate `F6.2` against SEC-15 release checklist and parent index links.
5. Document `F6.2` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `F6.2`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids F6.2` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §8 | Master hierarchy |
| [F6-index](F6-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids F6.2` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [F6-index](F6-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
