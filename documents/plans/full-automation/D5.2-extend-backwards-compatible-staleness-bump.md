<!-- Complete pass 3 2026-06-28 D5.2 -->

# D5.2: extend backwards-compatible staleness bump

**Parent:** [D5-index](D5-index.md) · **Branch D** · **Vision §6** · **Release:** v2.16

## Reader narrative
<!-- prose-source: agent plane-d 2026-06-28 -->

Extend means backwards-compatible improvement to an existing catalog artifact—add optional flag to script, append playbook section, extend skill without breaking callers. Extension triggers staleness bump on dependent design nodes and task cards per Plane E reconcile rules.

Platform queue items targeting extend must cite parent catalog id and compatibility proof—tests green, validate-workflow passes. Extend is preferred over fork when compose-first can absorb the change ([B4.3](B4.3-compose-first-catalog-before-improvise.md)). Non-compatible behavior change is fork, not extend, even if filename stays the same—semver and provenance matter.

## Purpose

D5.2 defines extend backwards compatible staleness bump for the agent-driven expert system. Platform evolution — promotion ladder, parallel queue, reuse.
## Scope

- Owns `D5.2` only; siblings under `D5` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §6 — Branch D — Platform evolution plane (parallel queue)](../../full-automation-vision-and-hierarchy.md#6-branch-d-platform-evolution-plane-parallel-queue).

```
│   ├── D5.2 extend: backwards-compatible tool change + staleness bump
```
## Behavior / step logic
<!-- timeline-source: agent cursor-agent 2026-06-28 -->

1. When a platform queue item targets extend, a worker improves an existing catalog artifact with backwards-compatible changes—optional script flags, appended playbook sections, extended skills—without breaking callers.
2. The item must cite parent catalog id; the verifier runs tests and validate-workflow to prove compatibility before dequeue.
3. On land, [E5.1](E5.1-staleness-design-graph-staleness-json.md) bumps staleness on dependent design nodes and task cards per Plane E reconcile rules.
4. Extend is preferred over fork when [B4.3](B4.3-compose-first-catalog-before-improvise.md) compose-first can absorb the change; non-compatible behavior requires fork even if the filename stays the same.
5. If compatibility proof fails or dependents remain stale without reconcile routing, pursuit stops at H2—never mark extend done while stale design artifacts block implement.

## JSON example

```json
{
  "node": "D5.2",
  "description": "extend backwards compatible staleness bump",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `docs/playbooks/`
- `scripts/`
- `.cursor/skills/playbook-keeper/`
- `state.platform.promotion_queue`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Platform queue depth 0 but product blocked on missing playbook â€” D3.3 priority cut skips platform drain.
- Edge case `D5.2` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `D5.2`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Add `platform.promotion_queue[]` to state.json schema.
2. Scheduler in autopilot workflow: `(steps_total % K) == 0` â†’ platform turn.
3. playbook-keeper + script extraction skills dequeue promotion items.
4. Validate `D5.2` against SEC-15 release checklist and parent index links.
5. Document `D5.2` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `D5.2`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids D5.2` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §6 | Master hierarchy |
| [D5-index](D5-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids D5.2` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [D5-index](D5-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
