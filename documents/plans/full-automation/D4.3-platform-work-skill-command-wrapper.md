<!-- Complete pass 3 2026-06-28 D4.3 -->

# D4.3: platform work skill command wrapper

**Parent:** [D4-index](D4-index.md) · **Branch D** · **Vision §6** · **Release:** v2.16

## Reader narrative
<!-- prose-source: agent plane-d 2026-06-28 -->

Skill and command wrapper work packages L2 scripts plus journal-keeper hooks into L3 `.cursor/skills/` or `/commands` entry points—thin orchestration surfaces the conductor invokes by name. Wrappers encode allowed_reads, evidence requirements, and forbidden paths consistent with active role ([B5.1](B5.1-active-role-from-template-pack.md)).

Wrapper promotion does not duplicate script logic inside SKILL.md; it calls S0 scripts and cites playbook paths. New commands register in operator dashboard docs when user-facing. Failed wrapper verify leaves queue item open; partial SKILL without command or vice versa re-enqueues until both surfaces exist or pack policy declares skill-only.

## Purpose

D4.3 defines platform work skill command wrapper for the agent-driven expert system. Platform evolution — promotion ladder, parallel queue, reuse.
## Scope

- Owns `D4.3` only; siblings under `D4` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §6 — Branch D — Platform evolution plane (parallel queue)](../../full-automation-vision-and-hierarchy.md#6-branch-d-platform-evolution-plane-parallel-queue).

```
│   ├── D4.3 skill/command wrapper
```
## Behavior / step logic
<!-- timeline-source: agent cursor-agent 2026-06-28 -->

1. When the promotion queue targets skill or command wrapper work, an economy worker packages promoted L2 scripts plus journal-keeper hooks into `.cursor/skills/` or command entry points the conductor invokes by name.
2. Wrapper surfaces call S0 scripts by path, cite playbook locations, and encode allowed_reads, evidence requirements, and forbidden paths consistent with [B5.1](B5.1-active-role-from-template-pack.md)—never duplicating script logic inside SKILL.md.
3. User-facing commands register in operator dashboard docs when pack policy requires operator discoverability.
4. The verifier runs the promotion verify pattern; on pass the conductor dequeues the item and product turns may compose the new surface via [B4.3](B4.3-compose-first-catalog-before-improvise.md).
5. Failed wrapper verify, a partial SKILL without command, or pack binding mismatch leaves the item open—re-enqueue until both surfaces exist or pack policy declares skill-only.

## JSON example

```json
{
  "node": "D4.3",
  "description": "platform work skill command wrapper",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `docs/playbooks/`
- `scripts/`
- `.cursor/skills/playbook-keeper/`
- `state.platform.promotion_queue`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Platform queue depth 0 but product blocked on missing playbook â€” D3.3 priority cut skips platform drain.
- Edge case `D4.3` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `D4.3`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Add `platform.promotion_queue[]` to state.json schema.
2. Scheduler in autopilot workflow: `(steps_total % K) == 0` â†’ platform turn.
3. playbook-keeper + script extraction skills dequeue promotion items.
4. Validate `D4.3` against SEC-15 release checklist and parent index links.
5. Document `D4.3` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `D4.3`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids D4.3` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §6 | Master hierarchy |
| [D4-index](D4-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids D4.3` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [D4-index](D4-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
