<!-- Complete pass 3 2026-06-28 I3.2 -->

# I3.2: runtime headless GitHub Actions validate verify

**Parent:** [I3-index](I3-index.md) · **Branch I** · **Vision §11** · **Release:** v2.18

## Reader narrative
<!-- prose-source: agent plane-i 2026-06-28 -->

GitHub Actions (or equivalent CI) runs validate-workflow.py and headless-verify on pull requests—deterministic gates before merge without human babysitting each test. CI does not clear H3 sign-off or design gates in state.json; it validates repo conformance and task evidence where configured.

Workflow definitions should mirror local verify commands from task cards and pack verify suites ([F1.8](F1.8-pack-verify-suites.md)). CI failure comments link to evidence logs; passing CI is necessary but not sufficient for goal_verify.

## Purpose

I3.2 defines runtime headless github actions validate verify for the agent-driven expert system. Runtime & integration — IDE, SDK, headless, MCP tools.
## Scope

- Owns `I3.2` only; siblings under `I3` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §11 — Branch I — Runtime & integration plane](../../full-automation-vision-and-hierarchy.md#11-branch-i-runtime-integration-plane).

```
│   ├── I3.2 GitHub Actions validate + verify
```
## Behavior / step logic
<!-- timeline-source: agent cli-composer-2.5 2026-06-28 -->

1. At H1 plan approval, the conductor stamps goal_id, optional parent_goal, and goal_type into state.json and the journal—the identity record every pursuit wake reloads before routing or verification.
2. On each wake, [A2.1](A2.1-preflight-check-pipeline-blocked-extended.md) reads goal_type to bind contracts: milestone goals require manifest gates, feature goals bind task cards and evidence paths, and company_ops goals follow the ops queue.
3. When parent_goal is set, child pursuits inherit roll-up scope so parallel program streams report status to the parent without duplicating identity at each leaf or losing H3 roll-up.
4. The conductor and journal-keeper dual-write any H1 amendments to these fields; [B1.1](B1.1-s0-deterministic-mandatory-first.md) scripts refuse progression when goal_id collides or parent_goal points to a missing ancestor.
5. If identity drifts—duplicate ids, wrong parent—pursuit halts at H2, evidence must not attach to the wrong scope, and [A1.3](A1.3-goal-verify-command-meta-test.md) cannot pass until fields are reconciled.

## JSON example

```json
{
  "node": "I3.2",
  "description": "runtime headless github actions validate verify",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `docs/automation/`
- `.cursor/hooks/`
- `docs/operator/export-contract.md`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Edge case `I3.2` variant 3: verify state dual-write before continuing pursuit.
- Edge case `I3.2` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `I3.2`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Map `I3.2` to v2.14â€“v2.23 release row in SEC-15-index.md.
2. Create or extend S0 script if behavior is file-derived.
3. Add unit test under tests/unit/test_i3_2.py when script exists.
4. Validate `I3.2` against SEC-15 release checklist and parent index links.
5. Document `I3.2` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `I3.2`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids I3.2` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §11 | Master hierarchy |
| [I3-index](I3-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids I3.2` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [I3-index](I3-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
