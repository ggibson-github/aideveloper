<!-- Complete pass 3 2026-06-28 D3.1-1 -->

# D3.1-1: platform turn per k product turns

**Parent:** [D3-index](D3-index.md) · **Branch D** · **Vision §6** · **Release:** v2.16

## Reader narrative
<!-- prose-source: agent plane-d 2026-06-28 -->

Default scheduling grants one platform turn every K product turns—commonly K=5—encoded in `platform.drain_policy.product_steps_per_platform_turn`. Autopilot evaluates `(steps_total % K) == 0` after each product phase completes; when true, the next iteration is platform dequeue instead of implement or design.

If the improvement queue is empty, the platform turn is skipped and the project continues—delivery does not sit idle waiting for self-improvement work. K is pack-configurable ([D5.1](D5.1-configure-task-level-params-only.md)) without forking schedulers. Overrides from [D3.2](D3.2-priority-boost-queue-depth-threshold.md), [D3.4](D3.4-idle-drain-product-waits-h2.md), and [D3.5](D3.5-max-platform-backlog-age-force-drain.md) layer on this baseline.

## Purpose

D3.1 defines 1 platform turn per k product turns for the agent-driven expert system. Platform evolution — promotion ladder, parallel queue, reuse.
## Scope

- Owns `D3.1` only; siblings under `D3` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §6 — Branch D — Platform evolution plane (parallel queue)](../../full-automation-vision-and-hierarchy.md#6-branch-d-platform-evolution-plane-parallel-queue).

```
│   ├── D3.1 default: 1 platform turn per K product turns (e.g. K=5)
```
## Behavior / step logic
<!-- timeline-source: agent cli-composer-2.5 2026-06-28 -->

1. When compose-first cannot extend a catalog entry without breaking consumers or pack isolation, the conductor records the divergence in [B4.4](B4.4-divergence-log-when-not-composing.md) and opens a fork with provenance back to the source promotion or parent artifact.
2. Platform workers allocate a new catalog id, append an INDEX row, and wire a staleness graph node per [D6.4](D6.4-platform-done-staleness-node-wired.md) before marking the fork item done in the promotion queue.
3. Downstream task cards migrate explicitly from the old id to the new entry; silent aliasing is forbidden so goal_verify evidence remains tied to one canonical component.
4. Pack fragments from [D4.6](D4.6-platform-work-pack-fragment-export.md) may fork upward into the template-pack namespace when company boundaries require a separate reusable artifact.
5. Fork without provenance fails closed at H2 and blocks SEC-14 gap-analysis trust rather than publishing an untraceable catalog entry.

## JSON example

```json
{
  "node": "D3.1",
  "description": "1 platform turn per k product turns",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## State / data fields

| Field | Type | Description |
|-------|------|-------------|
| `platform.drain_policy.product_steps_per_platform_turn` | number | Default K=5 |

## Repo artifacts (this branch)

- `docs/playbooks/`
- `scripts/`
- `.cursor/skills/playbook-keeper/`
- `state.platform.promotion_queue`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Platform queue depth 0 but product blocked on missing playbook â€” D3.3 priority cut skips platform drain.
- Edge case `D3.1` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `D3.1`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Add `platform.promotion_queue[]` to state.json schema.
2. Scheduler in autopilot workflow: `(steps_total % K) == 0` â†’ platform turn.
3. playbook-keeper + script extraction skills dequeue promotion items.
4. Validate `D3.1` against SEC-15 release checklist and parent index links.
5. Document `D3.1` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `D3.1`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids D3.1` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §6 | Master hierarchy |
| [D3-index](D3-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids D3.1` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [D3-index](D3-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
