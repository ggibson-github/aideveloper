<!-- Complete pass 3 2026-06-28 G5.2 -->

# G5.2: mistake scope creep allowed_reads task card

**Parent:** [G5-index](G5-index.md) · **Branch G** · **Vision §9** · **Release:** exists

## Reader narrative
<!-- prose-source: agent plane-g 2026-06-28 -->

Scope creep is editing paths outside `allowed_reads` or beyond the task card boundary—often from workers opening full design trees or “helpful” refactors. Controls: Librarian caps ([B2.2](B2.2-librarian-allowed-reads-catalog-composition.md)), spawn contracts, and conductor merge that rejects out-of-scope diffs.

Task cards list explicit file targets; implement-feature workers must not expand. Violations trigger rework, not silent acceptance; repeated creep may escalate H2 with diff evidence.

## Purpose

G5.2 defines mistake scope creep allowed reads task card for the agent-driven expert system. Verification & quality — evidence, goal_verify, anti-mistake.
## Scope

- Owns `G5.2` only; siblings under `G5` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §9 — Branch G — Verification & quality plane (anti-mistake)](../../full-automation-vision-and-hierarchy.md#9-branch-g-verification-quality-plane-anti-mistake).

```
│   ├── G5.2 scope creep → allowed_reads + task card
```
## Behavior / step logic
<!-- timeline-source: agent cli-composer-2.5 2026-06-28 -->

1. When the operator invokes `/continue`, `/autopilot`, `/status`, `/gate`, `/task`, `/verify`, `/lane`, or `/program`, the IDE loads the matching skill from `.cursor/skills/` and runs [S0](B1.1-s0-deterministic-mandatory-first.md) scripts before the conductor LLM turn.
2. `/continue` executes exactly one pipeline phase per [A2.2](A2.2-if-ready-execute-one-pipeline-step.md) after preflight; `/autopilot` sets `autopilot.active` and loops until `check-pipeline-blocked` stops pursuit per [A3.2](A3.2-goal-autopilot-until-goal-verify-or-hard-block.md).
3. `/verify` routes to the verifier skill and `verify-router.py`; `/lane` and `/program` invoke orchestration skills—none of these commands clear `gates_pending` or substitute for H2 answers or H3 approval per [A5.2](A5.2-continue-not-approval-self-gate-h1-h3-only.md).
4. Command stubs under `.cursor/commands/` stay aligned with skill contracts so IDE sessions, [I2.1](I2.1-runtime-sdk-run-local-pipeline-goal-autopilot.md) SDK runners, and headless entry points share the same pursuit semantics.
5. If a slash command would advance past `hitl.pending` or design gates without explicit operator approval or waiver text in the journal, pursuit fail-closes at H2 rather than improvising.

## JSON example

```json
{
  "node": "G5.2",
  "description": "mistake scope creep allowed reads task card",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `scripts/verify-router.py`
- `scripts/validate-workflow.py`
- `evidence/`
- `.cursor/skills/verifier/`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Flaky test â€” escalation S4 once, then H2 with evidence log; no silent retry loop.
- Edge case `G5.2` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `G5.2`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Extend verify-router for goal-level suite invocation.
2. Wire CI: validate-workflow checks goal block when pursuit.mode=goal_autopilot.
3. Document evidence type in docs/operator/evidence-types.md.
4. Validate `G5.2` against SEC-15 release checklist and parent index links.
5. Document `G5.2` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `G5.2`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids G5.2` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §9 | Master hierarchy |
| [G5-index](G5-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids G5.2` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [G5-index](G5-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
