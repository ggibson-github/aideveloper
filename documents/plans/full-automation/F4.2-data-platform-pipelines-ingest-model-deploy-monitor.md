<!-- Complete pass 3 2026-06-28 F4.2 -->

# F4.2: data platform pipelines ingest model deploy monitor

**Parent:** [F4-index](F4-index.md) · **Branch F** · **Vision §8** · **Release:** v2.21

## Reader narrative
<!-- prose-source: agent plane-f 2026-06-28 -->

Data platform pipelines follow ingest → model/transform → deploy → monitor/alert—phases mapped to engineer, DBA, and SRE roles with governance checkpoints. Deploy phases require evidence bundles and may block outside maintenance windows per pack policy.

Manifest and artifact graph ([F1.4](F1.4-pack-manifest-md-integration-contracts.md), [F1.5](F1.5-pack-artifact-graph-json-cross-role.md)) wire schema approval before deploy lanes start. Monitor phase feeds goal_verify metrics checks in [F1.8](F1.8-pack-verify-goal-verify-suites.md). S0 scripts should validate pipeline YAML against pack schema before instantiation saves bad graphs to state.

## Purpose

F4.2 defines data platform pipelines ingest model deploy monitor for the agent-driven expert system. Organization — template-packs as whole-company ceiling.
## Scope

- Owns `F4.2` only; siblings under `F4` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §8 — Branch F — Organization plane (template-packs = ceiling)](../../full-automation-vision-and-hierarchy.md#8-branch-f-organization-plane-template-packs-ceiling).

```
│   └── F4.2 pipelines: ingest → model → deploy → monitor
```
## Behavior / step logic
<!-- timeline-source: agent cursor-agent 2026-06-28 -->

1. When a data-platform pack goal activates, [F2.2](F2.2-company-spawn-workstream-department-role-lane.md) maps ingest, model/transform, deploy, and monitor phases to engineer, DBA, and SRE lanes with governance checkpoints before parallel work starts.
2. Before deploy lanes dequeue, [F1.4](F1.4-pack-manifest-md-integration-contracts.md) manifest approval and [F1.5](F1.5-pack-artifact-graph-json-cross-role.md) artifact-graph edges must show schema sign-off—S0 validates pipeline YAML against pack schema so bad graphs never land in state.json.
3. Deploy-phase pursue steps require evidence bundles per pack policy; the conductor blocks deploy outside approved maintenance windows until H1 waives or the window opens.
4. Monitor-phase outputs feed [F1.8](F1.8-pack-verify-goal-verify-suites.md) metrics checks so goal_verify aggregates pipeline health without subjective review.
5. If schema validation fails, manifest edges are missing, or deploy evidence is incomplete, pursuit fails closed at H2—lanes do not advance to deploy or monitor until proof is attached.

## JSON example

```json
{
  "node": "F4.2",
  "description": "data platform pipelines ingest model deploy monitor",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `template-packs/`
- `program/integration/manifest.md`
- `.cursor/skills/program-scoper/`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Pack role handoff while lane lease held â€” complete-work-order releases lease before role switch.
- Edge case `F4.2` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `F4.2`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Add `company.yaml` + `roles/*.yaml` to template-packs schema.
2. program-scoper selects pack; sets state.company.active_role.
3. Per-role allowed_reads in lane.json work orders.
4. Validate `F4.2` against SEC-15 release checklist and parent index links.
5. Document `F4.2` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `F4.2`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids F4.2` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §8 | Master hierarchy |
| [F4-index](F4-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids F4.2` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [F4-index](F4-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
