<!-- Complete pass 3 2026-06-28 E3.3 -->

# E3.3: remember skill INDEX no FTS global

**Parent:** [E3-index](E3-index.md) · **Branch E** · **Vision §7** · **Release:** v2.17

## Reader narrative
<!-- prose-source: agent plane-e 2026-06-28 -->

The remember skill captures operator snippets into `docs/facts/captured.md` and updates facts INDEX—explicit "remember this" workflow, not ambient chat memory. There is no global FTS or hidden vector store; all retained knowledge is file-backed and discoverable via [E1.4](E1.4-catalog-facts-index.md).

This keeps Plane E honest: if it is not in facts/decisions/catalog, agents should not treat it as project truth. Remember complements journal Resolved Q&A for durable non-decision snippets (Slack URLs, SQL fragments). Promotion candidates from repeated captures may enqueue via [E2.5](E2.5-compose-miss-l0-enqueue-promotion.md).

## Purpose

E3.3 defines remember skill index no fts global for the agent-driven expert system. Knowledge & composition — catalog, compose-first, staleness.
## Scope

- Owns `E3.3` only; siblings under `E3` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §7 — Branch E — Knowledge & composition plane](../../full-automation-vision-and-hierarchy.md#7-branch-e-knowledge-composition-plane).

```
│   └── E3.3 remember skill → INDEX (no FTS global memory)
```
## Behavior / step logic
<!-- timeline-source: agent cli-composer-2.5 2026-06-28 -->

1. When `evidence_required` is true in state.json per [G1.2](G1.2-evidence-required-state.md), the conductor runs [G1.1](G1.1-task-verify-router-verifier.md) verify-router for each implement task and blocks advancing `next_action` until `last_verify=passed` and evidence paths exist on disk.
2. Workers may not mark TODOS or task cards complete in prose—the conductor dual-writes completion only after verify-router exit 0 and evidence files are recorded in journal and state.
3. S0 `validate-workflow.py` checks `last_verify` and `evidence_files` on every wake; mismatch between claimed done and machine state triggers an integrity stop per [A4.4](A4.4-stop-integrity-validate-workflow-state-corrupt.md).
4. Audit and hierarchy quality scripts re-enqueue deepen when queue items close without artifacts, preventing silent hallucinated done from persisting across passes.
5. If pursuit would advance without demonstrated evidence, the conductor fails closed at H2 rather than assuming success—Plane G treats demonstrated verify as the only valid completion signal.

## JSON example

```json
{
  "node": "E3.3",
  "description": "remember skill index no fts global",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `docs/facts/INDEX.md`
- `docs/playbooks/INDEX.md`
- `docs/manifest/staleness.json`
- `allowed_reads`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Edge case `E3.3` variant 3: verify state dual-write before continuing pursuit.
- Edge case `E3.3` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `E3.3`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Map `E3.3` to v2.14â€“v2.23 release row in SEC-15-index.md.
2. Create or extend S0 script if behavior is file-derived.
3. Add unit test under tests/unit/test_e3_3.py when script exists.
4. Validate `E3.3` against SEC-15 release checklist and parent index links.
5. Document `E3.3` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `E3.3`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids E3.3` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §7 | Master hierarchy |
| [E3-index](E3-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids E3.3` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [E3-index](E3-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
