<!-- Complete pass 3 2026-06-28 C2.4 -->

# C2.4: phase test refactor git-workflow

**Parent:** [C2-index](C2-index.md) · **Branch C** · **Vision §5** · **Release:** exists

## Reader narrative
<!-- prose-source: agent plane-c 2026-06-28 -->

Test and git-workflow phase covers test-writer, full suite runs before push, refactor when debt blocks features, and PR/commit when user requests. test-before-push rule applies: unit, integration, e2e when UI exists.

git-workflow runs only when last_verify passed or exceptions documented. This phase connects Plane C delivery to operator git policy without bypassing evidence gates.

## Purpose

C2.4 defines phase test refactor git workflow for the agent-driven expert system. Product execution — pipelines, tasks, program, delivery.
## Scope

- Owns `C2.4` only; siblings under `C2` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §5 — Branch C — Product execution plane](../../full-automation-vision-and-hierarchy.md#5-branch-c-product-execution-plane).

```
│   ├── C2.4 test / refactor / git-workflow
```
## Behavior / step logic
<!-- timeline-source: agent cli-composer-2.5 2026-06-28 -->

1. During task-breakdown after DD approval, the conductor sequences environment setup tasks—portable Python, venv, Visual Studio solution, GitHub clone—before any library or package install tasks in TODOS and task card dependencies.
2. Implement-feature workers execute env-setup cards first; pip, NuGet, or package installs run only inside or against the runtime established by those completed setup tasks per AGENTS.md rule 17.
3. Task card dependency edges and audit scripts verify ordering—an implement card cannot advance while its prerequisite env-setup card lacks `last_verify=passed` and evidence on disk.
4. When scaffold or pack [C1.4](C1.4-pack-defined-pipelines-per-company-template.md) introduces new runtime requirements, task-breakdown inserts additional env-setup rows at the front of the breakdown before downstream implement turns route from state.json.
5. If library install tasks run before env setup completes, local runnability checks fail, or task dependencies skip setup ordering, pursuit fails closed at H2 until env-setup evidence passes and task card order is reconciled.

## JSON example

```json
{
  "node": "C2.4",
  "description": "phase test refactor git workflow",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `docs/tasks/`
- `docs/manifest/pipelines/`
- `.cursor/skills/implement-feature/`
- `program/workstreams/`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Edge case `C2.4` variant 3: verify state dual-write before continuing pursuit.
- Edge case `C2.4` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `C2.4`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Map `C2.4` to v2.14â€“v2.23 release row in SEC-15-index.md.
2. Create or extend S0 script if behavior is file-derived.
3. Add unit test under tests/unit/test_c2_4.py when script exists.
4. Validate `C2.4` against SEC-15 release checklist and parent index links.
5. Document `C2.4` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `C2.4`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids C2.4` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §5 | Master hierarchy |
| [C2-index](C2-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids C2.4` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [C2-index](C2-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
