<!-- Complete pass 3 2026-06-28 D4.2 -->

# D4.2: platform work script extraction

**Parent:** [D4-index](D4-index.md) · **Branch D** · **Vision §6** · **Release:** v2.16

## Reader narrative
<!-- prose-source: agent plane-d 2026-06-28 -->

Script extraction promotes L1 playbook steps—or verified manual commands—into S0 Python under `scripts/` with tests. Extraction is platform work: changes tooling for all future goals, not the current consumer feature branch unless explicitly shared.

Extracted scripts must be idempotent, CLI-invokable, and referenced from playbook diff. Wire verify-router when script replaces task-card Test command. Backwards-compatible extensions use [D5.2](D5.2-extend-backwards-compatible-staleness-bump.md); behavior breaks require [D5.3](D5.3-fork-new-catalog-entry-provenance.md) fork with new catalog id. [B1.1](B1.1-s0-deterministic-mandatory-first.md) mandates conductor runs script before model re-derivation.

## Purpose

D4.2 defines platform work script extraction for the agent-driven expert system. Platform evolution — promotion ladder, parallel queue, reuse.
## Scope

- Owns `D4.2` only; siblings under `D4` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §6 — Branch D — Platform evolution plane (parallel queue)](../../full-automation-vision-and-hierarchy.md#6-branch-d-platform-evolution-plane-parallel-queue).

```
│   ├── D4.2 script extraction from playbook
```
## Behavior / step logic
<!-- timeline-source: agent cursor-agent 2026-06-28 -->

1. On platform drain for script-extraction items, an economy worker promotes stable L1 playbook steps or verified manual commands into idempotent S0 Python under `scripts/` with tests.
2. The worker wires verify-router when the extracted script replaces a task-card Test command so future implement turns invoke S0 instead of ad-hoc shell.
3. Playbook diff references the new script path; the conductor enforces [B1.1](B1.1-s0-deterministic-mandatory-first.md)—run the script before the model re-derives the same behavior.
4. Backwards-compatible behavior changes follow [D5.2](D5.2-extend-backwards-compatible-staleness-bump.md); breaking changes require [D5.3](D5.3-fork-new-catalog-entry-provenance.md) fork with new catalog id and provenance.
5. If extraction lacks tests, a CLI entry point, or playbook linkage, the queue item stays open and pursuit blocks false L2 promotion at H2.

## JSON example

```json
{
  "node": "D4.2",
  "description": "platform work script extraction",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `docs/playbooks/`
- `scripts/`
- `.cursor/skills/playbook-keeper/`
- `state.platform.promotion_queue`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Platform queue depth 0 but product blocked on missing playbook â€” D3.3 priority cut skips platform drain.
- Edge case `D4.2` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `D4.2`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Add `platform.promotion_queue[]` to state.json schema.
2. Scheduler in autopilot workflow: `(steps_total % K) == 0` â†’ platform turn.
3. playbook-keeper + script extraction skills dequeue promotion items.
4. Validate `D4.2` against SEC-15 release checklist and parent index links.
5. Document `D4.2` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `D4.2`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids D4.2` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §6 | Master hierarchy |
| [D4-index](D4-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids D4.2` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [D4-index](D4-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
