<!-- Complete pass 3 2026-06-28 D1.2 -->

# D1.2: L1 playbook docs/playbooks

**Parent:** [D1-index](D1-index.md) · **Branch D** · **Vision §6** · **Release:** v2.16

## Reader narrative
<!-- prose-source: agent plane-d 2026-06-28 -->

L1 promotes distilled procedure into `docs/playbooks/<slug>.md`: step lists, constraints, and pointers that humans and agents can follow without re-reading full design monoliths. Playbook-keeper skill ([D4.1](D4.1-platform-work-playbook-keeper.md)) is the primary platform worker for L1 creation from divergence logs and task post-mortems.

An L1 playbook is not a script—it encodes intent and ordering while leaving file-derived steps to future L2 extraction. INDEX rows and task-card references ([D6.1](D6.1-platform-done-catalog-index-row.md), [D6.3](D6.3-platform-done-task-card-references.md)) prove the playbook is discoverable. Stale upstream design should bump dependents via Plane E staleness, not silent drift.

## Purpose

D1.2 defines l1 playbook docs playbooks for the agent-driven expert system. Platform evolution — promotion ladder, parallel queue, reuse.
## Scope

- Owns `D1.2` only; siblings under `D1` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §6 — Branch D — Platform evolution plane (parallel queue)](../../full-automation-vision-and-hierarchy.md#6-branch-d-platform-evolution-plane-parallel-queue).

```
│   ├── D1.2 L1 playbook (docs/playbooks/<slug>.md)
```
## Behavior / step logic
<!-- timeline-source: agent cli-composer-2.5 2026-06-28 -->

1. Every conductor and worker session loads always-on context—[E4.1](E4.1-context-always-on-rules-agents-md.md) rules and `AGENTS.md`—as layer zero before hooks ([E4.2](E4.2-context-hooks-inject-continue-start.md)) or Librarian `allowed_reads` ([E4.3](E4.3-context-allowed-reads-cap-max-5.md)) narrow the turn.
2. `AGENTS.md` encodes the baseline pursuit contract: evidence gates, H1/H2/H3 approval semantics, journal dual-write, and pipeline phase order that the genius conductor enforces each wake.
3. Economy workers inherit applicable rule excerpts in spawn context; they do not re-read the full `AGENTS.md` monolith unless the conductor bound it in `allowed_reads`.
4. Active template packs may extend always-on rules via pack fragments merged at role switch so company-specific policy layers atop repo defaults without replacing core conductor obligations.
5. When `AGENTS.md` or always-on rules change, [E5.2](E5.2-staleness-extend-playbooks-scripts-pack-nodes.md) marks dependent playbooks and skills stale and pursuit pauses at H2 until reconcile-stale or operator H1 confirms downstream artifacts are current.

## JSON example

```json
{
  "node": "D1.2",
  "description": "l1 playbook docs playbooks",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `docs/playbooks/`
- `scripts/`
- `.cursor/skills/playbook-keeper/`
- `state.platform.promotion_queue`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Platform queue depth 0 but product blocked on missing playbook â€” D3.3 priority cut skips platform drain.
- Edge case `D1.2` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `D1.2`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Add `platform.promotion_queue[]` to state.json schema.
2. Scheduler in autopilot workflow: `(steps_total % K) == 0` â†’ platform turn.
3. playbook-keeper + script extraction skills dequeue promotion items.
4. Validate `D1.2` against SEC-15 release checklist and parent index links.
5. Document `D1.2` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `D1.2`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids D1.2` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §6 | Master hierarchy |
| [D1-index](D1-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids D1.2` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [D1-index](D1-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
