<!-- Complete pass 3 2026-06-28 B2.3 -->

# B2.3: Phase workers implement design explore

**Parent:** [B2-index](B2-index.md) · **Branch B** · **Vision §4** · **Release:** exists

## Reader narrative
<!-- prose-source: agent plane-b 2026-06-28 -->

Phase workers implement one skill phase under contract: implement-feature, dd-writer, explore, refactor—each with task card paths, evidence requirements, and no journal writes. The conductor selects the worker type from `next_action` and route-tier output.

Parallel phase workers are allowed only when the integration manifest declares independent lanes; otherwise one worker per turn preserves auditability. Workers return summaries; the conductor merges and verifies before marking progress.

## Purpose

B2.3 defines phase workers implement design explore for the agent-driven expert system. Cognition & routing — [S0](B1.1-s0-deterministic-mandatory-first.md)–S4, conductor, workers, dual-stack.
## Scope

- Owns `B2.3` only; siblings under `B2` must not duplicate this spec.
- Aligns with minimal HITL: H1 plan, H2 blocker, H3 sign-off ([INTRO-1.2](INTRO-1.2-human-touchpoint-contract-h1-h2-h3.md)).
- Conflicts resolve in favor of [Vision §4 — Branch B — Cognition & routing plane](../../full-automation-vision-and-hierarchy.md#4-branch-b-cognition-routing-plane).

```
│   ├── B2.3 Phase workers: implement / design / explore
```
## Behavior / step logic
<!-- timeline-source: agent cursor-agent 2026-06-28 -->

1. From `next_action`, the conductor spawns one phase worker type per turn—implement-feature, dd-writer, librarian explore, refactor—each under a task card or skill contract with evidence requirements and no journal writes.
2. Parallel phase workers run only when the integration manifest declares independent lanes; otherwise one worker per turn preserves auditability and avoids conflicting dual-write.
3. Workers execute the scoped phase, return summaries and evidence paths, and must not choose the next pipeline step or update `journal/state.json` themselves.
4. The conductor verifies worker output against task card acceptance criteria before marking phase progress or spawning [B2.4](B2.4-verifier-tool-operator-evidence.md) for command proof.
5. If a worker expands beyond `allowed_reads`, omits required evidence, or attempts journal writes, pursuit blocks at H2 until the conductor rescopes or respawns under a corrected contract.

## JSON example

```json
{
  "node": "B2.3",
  "description": "phase workers implement design explore",
  "state": { "ref": "APP-B-state-json-sketch.md" },
  "implemented_in_release": "v2.14+"
}
```


## Repo artifacts (this branch)

- `docs/operator/model-policy.json`
- `scripts/route-tier.py`
- `.cursor/skills/librarian/`
- `.cursor/rules/genius-conductor.mdc`

## Edge cases

- Operator closes laptop mid-loop â€” state.json must resume from last good dual-write.
- Concurrent manual edit to queue JSON â€” conductor reloads queue each wake; last writer wins with journal note.
- Edge case `B2.3` variant 3: verify state dual-write before continuing pursuit.
- Edge case `B2.3` variant 4: verify state dual-write before continuing pursuit.
- Pass 3: add regression test or evidence path specific to `B2.3`.
- Pass 3: cross-link related nodes in same branch index.

## Failure modes

- **Silent stop:** Agent ends turn without updating queue â†’ mitigated by /loop + check-hierarchy-queue.py EMPTY gate.
- **False complete:** Item marked done without artifact â†’ audit-hierarchy-depth.py re-enqueues deepen pass.
- **Scope bleed:** Worker edits journal/state during planning-only expansion â†’ forbidden in vision-expansion-prompt.
- **Stale design:** Upstream vision Â§ changes â†’ reconcile-stale adds deepen items for affected ids.

## Concrete implementation

1. Map `B2.3` to v2.14â€“v2.23 release row in SEC-15-index.md.
2. Create or extend S0 script if behavior is file-derived.
3. Add unit test under tests/unit/test_b2_3.py when script exists.
4. Validate `B2.3` against SEC-15 release checklist and parent index links.
5. Document `B2.3` in parent index with verify command and release tag.
6. Add checklist row in SEC-15 release doc for `B2.3`.

## Verification

| Check | Command |
|-------|---------|
| Completeness | `python scripts/automation/audit-hierarchy-depth.py --strict --ids B2.3` |
| Conformance | `python scripts/validate-workflow.py` |
| Task evidence | `python scripts/verify-router.py` when implement task exists |

## Dependencies

| Link | Why |
|------|-----|
| [full-automation-vision-and-hierarchy.md](../../full-automation-vision-and-hierarchy.md) §4 | Master hierarchy |
| [B2-index](B2-index.md) | Parent grouping |
| [genius-conductor-tiered-routing.md](../../genius-conductor-tiered-routing.md) | S0–S4 routing |

## Acceptance criteria

- [ ] `python scripts/automation/audit-hierarchy-depth.py --strict --ids B2.3` passes
- [ ] Named script, skill, or test path exists or is listed in SEC-15 release row
- [ ] Linked from [B2-index](B2-index.md)
- [ ] `python scripts/validate-workflow.py` passes after implement

## Cross-links

- [hierarchy-expander SKILL](../../../.cursor/skills/hierarchy-expander/SKILL.md)
